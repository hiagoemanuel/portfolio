import styled from 'styled-components'

export const ThemeButtonStyle = styled.div`
    align-self: flex-start;
    user-select: none;
    cursor: pointer;

    .hidden { display: none; }
    
    svg {
        width: 100%; 
        height: 100%;
    }
`